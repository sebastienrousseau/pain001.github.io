*.DS_Store
*.profraw
/.vscode/
/output/
/public/
/target/
build
Icon?
src/.DS_Store
tarpaulin-report.html
.ssg-cache/
.ssg-plugin-cache.json
# The built site. Deployed from ci.yml as a Pages artifact, never committed.
site/

# Local-only accessibility tooling (see scripts/a11y_local.mjs)
.a11y-tools/
# Fetched and hash-verified at build time by scripts/fetch_runtime.py from
# the URLs pinned in static/pyodide/pain001-runtime.json; never committed.
static/pyodide/*.whl
static/pyodide/*.wasm
static/pyodide/python_stdlib.zip

# Installed from package-lock.json by `npm ci`; never committed.
node_modules/

# Release archive, SBOM copy and checksums, built by release.yml and by anyone
# reproducing it locally. Published as release assets, never committed.
dist/
